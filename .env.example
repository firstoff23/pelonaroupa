# Local frontend/backend example values. Do not commit real secrets.

# Primary ML backend (Fly.dev — always tried first, 5s timeout):
# FASTAPI_BACKEND_URL is kept for legacy reference; primary URL is now hardcoded in routers.ts
# FASTAPI_BACKEND_URL=https://animalmind-backend.fly.dev

# Secondary ML backend (Hugging Face Space — tried if Fly.dev fails):
HF_SPACE_URL=https://firstoff-animalmind-backend.hf.space

# Frontend reference to FastAPI (for posture/species detection in RecordingPage):
# VITE_FASTAPI_URL=https://animalmind-backend.fly.dev
# VITE_HF_SPACE_URL=https://firstoff-animalmind-backend.hf.space

# Local FastAPI backend (development only):
# FASTAPI_BACKEND_URL=http://localhost:8000
